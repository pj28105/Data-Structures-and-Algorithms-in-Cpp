#include<bits/stdc++.h>
using namespace std;

#define fast ios_base::sync_with_stdio(false),cin.tie(NULL),cout.tie(NULL);
#define tc int t; cin >> t; for(int test = 1; test <= t; test++)

typedef long long ll;

const ll inf = 1e9; 
const int mod = 1e9 + 7;

int main(void){
    fast;
    #ifndef ONLINE_JUDGE
        freopen("../in.txt","r",stdin);
    #endif
}